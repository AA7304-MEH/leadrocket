{
    "name": "leadrockets-monorepo",
    "version": "4.0.0",
    "private": true,
    "scripts": {
        "dev": "turbo run dev --parallel",
        "build": "turbo run build",
        "lint": "turbo run lint",
        "clean": "turbo run clean"
    },
    "devDependencies": {
        "concurrently": "^8.0.0",
        "turbo": "^1.10.0"
    },
    "workspaces": [
        "apps/*"
    ],
    "dependencies": {
        "hpp": "^0.2.3",
        "xss-clean": "^0.1.4"
    }
}
